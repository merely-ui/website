import { FlexPage } from '@/src/screens/flex'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Flex | Merely UI',
}

export default function Flex() {
	return <FlexPage />
}
