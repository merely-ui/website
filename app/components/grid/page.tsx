import { GridPage } from '@/src/screens/grid'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Grid | Merely UI',
}

export default function Grid() {
	return <GridPage />
}
