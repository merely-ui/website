import { ComponentsPage } from '@/src/screens/components'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Components | Merely UI',
}

export default function Components() {
	return <ComponentsPage />
}
