import { InputPage } from '@/src/screens/input'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Input | Merely UI',
}

export default function Input() {
	return <InputPage />
}
