import { ModalPage } from '@/src/screens/modal'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Modal | Merely UI',
}

export default function Modal() {
	return <ModalPage />
}
