import { CardPage } from '@/src/screens/card'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Card | Merely UI',
}

export default function Card() {
	return <CardPage />
}
