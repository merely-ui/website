import { TemplatePage } from '@/src/screens/template'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Template | Merely UI',
}

export default function Template() {
	return <TemplatePage />
}
