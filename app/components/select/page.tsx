import { SelectPage } from '@/src/screens/select'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Select | Merely UI',
}

export default function Select() {
	return <SelectPage />
}
