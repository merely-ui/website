import { HintPage } from '@/src/screens/hint'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Hint | Merely UI',
}

export default function Hint() {
	return <HintPage />
}
