import { ButtonPage } from '@/src/screens/button'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Button | Merely UI',
}

export default function Button() {
	return <ButtonPage />
}
