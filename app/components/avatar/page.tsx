import { AvatarPage } from '@/src/screens/avatar'
import { Metadata } from 'next'

export const metadata: Metadata = {
	title: 'Avatar | Merely UI',
}

export default function Avatar() {
	return <AvatarPage />
}
