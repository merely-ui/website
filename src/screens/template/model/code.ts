export const ImportCode = `import { Template } from "@merely-ui/react"
import { Box } from "@merely-ui/react"`

export const UsageCode = `<Flex>
	<Template w={30} height={30} bgColor={colors.cyan.$200}  />
	<Box w={30} height={30} bgColor={colors.cyan.$200}  />
</Flex>`
